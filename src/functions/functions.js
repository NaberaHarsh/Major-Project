import React from 'react'
// onChange={(e)=>{e.target.value=='paytm'?this.setState({visible:{display:'block'}}):this.setState({visible:{display:'none'}})}}

class Function extends React.Component{

    render(){
        return(
            <div>

            </div>
        )
    }
}
export default Function;